@model IEnumerable<HospitalProject.Models.Speciality>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>


@if (User.Claims.ToList().Where(p => p.Value == "admin").SingleOrDefault() != null)
{
    <p>
        <a asp-action="Create">Create New</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.specialityId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.specialityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.specialityId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.specialityName)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.specialityId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.specialityId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.specialityId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
